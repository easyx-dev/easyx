---
title: AI Rich Editor 演示
description: AI Rich Editor 对话生成 HTML 片段与实时预览完整演示
---

import IframeDemo from '../../../components/IframeDemo.tsx';
import { getDemoSource } from '../../../components/demo-sources.ts';

`@easyx/ai-rich-editor` 的完整工作台演示：左侧预览、右侧对话，顶栏可打开代码编辑器。

<IframeDemo slug="ai-rich-editor-demo" sourceCode={getDemoSource('ai-rich-editor-demo')} client:load />

## 说明

- 文档站是纯静态站点、没有服务端，因此演示在浏览器侧拦截演示端点，**回放一段预录的 TanStack AI 标准 SSE 流**（逐字下发）。对话链路本身走真实实现：`fetchServerSentEvents` 消费 SSE、`createChatHook` 驱动消息渲染、流式同步到编辑器与预览
- 输入任意内容或点击推荐指令即可触发回放；回复中的 ```html 代码块可点击「应用到编辑器」
- 启用「编辑器」开关后，左栏变为 `[代码编辑器 | 预览]`，可手动改写生成的片段
- 演示注入了本地 `media` 配置：上传返回同源 Blob 地址、媒体库为内置示例图，因此**粘贴图片到输入框、拖文件进代码区、行号旁的「+」**都能真实跑通（无需服务端）
- 真实接入时把 `endpointUrl` 指向宿主的 SSE 端点、`media` 指向宿主的上传与媒体库接口即可，组件侧无需任何改动

## 库能力

- **对话生成**：基于 TanStack AI headless UI，渲染侧为包内自研（消息气泡 / 输入框 / 空态推荐 / 思考过程）
- **媒体插入**：对话侧粘贴 / 拖入 / 选择文件，发送时上传并把地址交给模型；代码面板侧「+」或拖入直接插入自包含片段
- **片段产出**：fragment-only，产物在应用时刻完成样式作用域化，宿主可直接当 HTML 引入
- **实时预览**：设备档位（桌面拉伸 / 手机固定设备框）、脚本开关、刷新、新窗口预览
- **代码编辑**：CodeMirror 6 按需加载，HTML 语法高亮（含内嵌 `<style>` / `<script>` 的 CSS 与 JS）、标签与属性补全、查找替换、行跳转
- **主题联动**：容器 class / 宿主 `data-theme` / 系统偏好三级判定，代码面板配色同步跟随
